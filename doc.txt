чат

создание таблицы бд, происходит одним из пользователей.
кто первый написал в чат и становится user_one.
таким образом создается один объект таблицы в бд:
чат
* chat_id

* user_one_id

* user_two_id

каждое сообщение заносится в отдельный объект таблицы message,
к которой мы обращаемся по id чата. Каждое сообщение представляет собой:

id_chat

id_message

sender_id

receiver_id

message - string

time - time

receiver_viewed - bool

Создаем таблицу со всеми юзерами и их основной информацией:

* user_id: генерим

* login: логин пользователя

* password: пароль пользователя

* first_name: имя пользователя

* last_name: фамилия пользователя



При добавлении информации доп информации:

* gender: гендер пользователя

* age: возраст

* image: фотографии char(150)[] хранним ссылки

* description: описание

* sex: сексуальные предпочтения

* tags: теги

* gps: локация пользователя

*fame_rating - автоматически выставляется

*online- автоматически

история кто лайкнул фотографию, кто посмотрел профиль, кто дизлакнул

*user_id -кому принадлежит таблица

*like_user массив кого юзер лайкнул

*liked массив кто лайкнул юзера

*block массив заблокированных пользователей юзером

*blocked массив кто заблокировал юзера
*history история посещений

